machine:
  services:
    - docker
  node:
      version:
        0.10.28

dependencies:
  override:
    - docker info
    - docker build -t vjclasgens/website:latest .
test:
  pre:
    - npm install
    - sleep 5
  override:
    - docker run -d -p 7000:7000 vjclasgens/website:latest sleep 2
    - npm run test-single-run
    - npm run protractor
deployment:




