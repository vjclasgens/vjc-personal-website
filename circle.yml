machine:
  services:
    - docker
  node:
      version:
        0.10.28

dependencies:
  override:
    - docker info
    - docker build -t vjclasgens/website:latest .
test:
  pre:
    - npm install
    - sleep 5
  override:
    - docker run -d -p 7000:7000 vjclasgens/website sleep 2
    - docker ps
    - curl $ANG_URL_TEST
    - npm run test-single-run
    - npm run protractor
deployment:




